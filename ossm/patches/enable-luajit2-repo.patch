diff --git a/bazel/deps.yaml b/bazel/deps.yaml
index 97efb00fe..28b93f29e 100644
--- a/bazel/deps.yaml
+++ b/bazel/deps.yaml
@@ -389,6 +389,21 @@ libevent:
   license: "BSD-3-Clause"
   license_url: "https://github.com/libevent/libevent/blob/{version}/LICENSE"

+luajit2:
+  project_name: "LuaJIT2"
+  project_desc: "OpenResty maintained branch of LuaJIT with s390x/ppc64le support"
+  project_url: "https://github.com/openresty/luajit2"
+  release_date: "2025-08-26"
+  use_category:
+  - dataplane_ext
+  extensions:
+  - envoy.filters.http.lua
+  - envoy.router.cluster_specifier_plugin.lua
+  - envoy.string_matcher.lua
+  cpe: "N/A"
+  license: "MIT"
+  license_url: "https://github.com/openresty/luajit2/blob/{version}/COPYRIGHT"
+
 luajit:
   project_name: "LuaJIT"
   project_desc: "Just-In-Time compiler for Lua"
diff --git a/bazel/foreign_cc/BUILD b/bazel/foreign_cc/BUILD
index f6b7b202a..019cd3cbb 100644
--- a/bazel/foreign_cc/BUILD
+++ b/bazel/foreign_cc/BUILD
@@ -122,6 +122,21 @@ configure_make(
     targets = [],
 )

+configure_make(
+    name = "luajit2",
+    configure_command = "luajit2_build.sh",
+    env = select({
+        "//bazel:asan_build": {"ENVOY_CONFIG_ASAN": "1"},
+        "//bazel:msan_build": {"ENVOY_CONFIG_MSAN": "1"},
+        "//conditions:default": {},
+    }),
+    lib_source = "@luajit2",
+    out_include_dir = "include/luajit-2.1",
+    out_static_libs = ["libluajit-5.1.a"],
+    tags = ["skip_on_windows"],
+    targets = [],
+)
+
 configure_make(
     name = "colm",
     autogen = True,
diff --git a/bazel/repositories.bzl b/bazel/repositories.bzl
index 02140e5c7..5ace181fa 100644
--- a/bazel/repositories.bzl
+++ b/bazel/repositories.bzl
@@ -197,6 +197,7 @@ def envoy_dependencies(skip_targets = []):
     _yaml_cpp()
     _libevent()
     _luajit()
+    _luajit2()
     _nghttp2()
     _msgpack_cxx()
     _cpp2sky()
@@ -924,6 +925,15 @@ def _luajit():
         patch_args = ["-p1"],
     )

+def _luajit2():
+    LUAJIT2_BUILD_CONTENT = """%s\nalias(name = "luajit2", actual = ":all", visibility = ["//visibility:public"])""" % BUILD_ALL_CONTENT
+    external_http_archive(
+        name = "luajit2",
+        build_file_content = LUAJIT2_BUILD_CONTENT,
+        patches = ["@envoy//bazel/foreign_cc:luajit2.patch"],
+        patch_args = ["-p1"],
+    )
+
 def _tcmalloc():
     external_http_archive(
         name = "tcmalloc",
diff --git a/bazel/repository_locations.bzl b/bazel/repository_locations.bzl
index 97dd2ba7b..93fc60807 100644
--- a/bazel/repository_locations.bzl
+++ b/bazel/repository_locations.bzl
@@ -253,6 +253,13 @@ REPOSITORY_LOCATIONS_SPEC = dict(
         strip_prefix = "LuaJIT-{version}",
         urls = ["https://github.com/LuaJIT/LuaJIT/archive/{version}.tar.gz"],
     ),
+    luajit2 = dict(
+        # OpenResty maintained branch of LuaJIT with s390x/ppc64le support
+        version = "dcc9c9ee67e1a5d3d636bd7745e95ddb4a1c70bc",
+        sha256 = "1e2374ac75618862d8c81bd5fc496fa7428c278164ad375a3e6b746e8833c0d2",
+        strip_prefix = "luajit2-{version}",
+        urls = ["https://github.com/openresty/luajit2/archive/{version}.tar.gz"],
+    ),
     nghttp2 = dict(
         version = "1.66.0",
         sha256 = "e178687730c207f3a659730096df192b52d3752786c068b8e5ee7aeb8edae05a",
diff --git a/source/extensions/filters/common/lua/BUILD b/source/extensions/filters/common/lua/BUILD
index 9b4cae878..2b53aa03a 100644
--- a/source/extensions/filters/common/lua/BUILD
+++ b/source/extensions/filters/common/lua/BUILD
@@ -1,3 +1,4 @@
+load("@bazel_skylib//rules:common_settings.bzl", "bool_flag")
 load(
     "//bazel:envoy_build_system.bzl",
     "envoy_cc_library",
@@ -8,19 +9,31 @@ licenses(["notice"])  # Apache 2

 envoy_extension_package()

+bool_flag(
+    name = "luajit2",
+    build_setting_default = False,
+)
+
+config_setting(
+    name = "luajit2_enabled",
+    flag_values = {":luajit2": "True"},
+)
+
 envoy_cc_library(
     name = "lua_lib",
     srcs = ["lua.cc"],
     hdrs = ["lua.h"],
     deps = [
-        "//bazel/foreign_cc:luajit",
         "//envoy/thread_local:thread_local_interface",
         "//source/common/common:assert_lib",
         "//source/common/common:c_smart_ptr_lib",
         "//source/common/common:lock_guard_lib",
         "//source/common/common:thread_lib",
         "//source/common/protobuf",
-    ],
+    ] + select({
+        ":luajit2_enabled": ["//bazel/foreign_cc:luajit2"],
+        "//conditions:default": ["//bazel/foreign_cc:luajit"],
+    }),
 )

 envoy_cc_library(
@@ -41,10 +54,12 @@ envoy_cc_library(
     hdrs = ["protobuf_converter.h"],
     deps = [
         ":lua_lib",
-        "//bazel/foreign_cc:luajit",
         "//source/common/protobuf",
         "//source/common/protobuf:create_reflectable_message_lib",
         "//source/common/protobuf:utility_lib",
         "@abseil-cpp//absl/strings",
-    ],
+    ] + select({
+        ":luajit2_enabled": ["//bazel/foreign_cc:luajit2"],
+        "//conditions:default": ["//bazel/foreign_cc:luajit"],
+    }),
 )
diff --git a/bazel/foreign_cc/luajit2.patch b/bazel/foreign_cc/luajit2.patch
new file mode 100644
index 00000000..00000000
--- /dev/null
+++ b/bazel/foreign_cc/luajit2.patch
@@ -0,0 +1,121 @@
+diff --git a/src/Makefile b/src/Makefile
+index acbe0ca7..cc1f2a43 100644
+--- a/src/Makefile
++++ b/src/Makefile
+@@ -27,7 +27,7 @@ NODOTABIVER= 51
+ DEFAULT_CC = gcc
+ #
+ # LuaJIT builds as a native 32 or 64 bit binary by default.
+-CC= $(DEFAULT_CC)
++CC ?= $(DEFAULT_CC)
+ #
+ # Use this if you want to force a 32 bit build on a 64 bit multilib OS.
+ #CC= $(DEFAULT_CC) -m32
+@@ -70,10 +70,10 @@ NODOTABIVER= 51
+ # as dynamic mode.
+ #
+ # Mixed mode creates a static + dynamic library and a statically linked luajit.
+-BUILDMODE= mixed
++#BUILDMODE= mixed
+ #
+ # Static mode creates a static library and a statically linked luajit.
+-#BUILDMODE= static
++BUILDMODE= static
+ #
+ # Dynamic mode creates a dynamic library and a dynamically linked luajit.
+ # Note: this executable will only run when the library is installed!
+diff --git a/luajit2_build.sh b/luajit2_build.sh
+new file mode 100755
+index 00000000..00000000
+--- /dev/null
++++ b/luajit2_build.sh
+@@ -0,0 +1,89 @@
++#!/bin/bash
++
++set -e
++
++PREFIX=""
++while [[ $# -gt 0 ]]; do
++    case $1 in
++        --prefix=*)
++            PREFIX="${1#*=}"
++            shift
++            ;;
++        --prefix)
++            PREFIX="$2"
++            shift 2
++            ;;
++        *)
++            shift
++            ;;
++    esac
++done
++
++# Copy source tree to a build directory
++SRC_DIR="$(dirname "$(realpath "$0")")"
++BUILD_DIR="$(basename "$SRC_DIR")_build"
++cp -r "$SRC_DIR" "$BUILD_DIR"
++cd "$BUILD_DIR"
++
++# Set environment variables
++export MACOSX_DEPLOYMENT_TARGET="10.8"
++export DEFAULT_CC="${CC:-}"
++export TARGET_CFLAGS="${CFLAGS:-} -fno-function-sections -fno-data-sections"
++EXTRA_MAKE_ARGS=()
++FUSE_LD_FLAG=$(echo "$LDFLAGS" | grep -o -- '-fuse-ld=[^ ]*' | tail -1 || :)
++SYSROOT_FLAG=$(echo "$LDFLAGS" | grep -o -- '--sysroot=[^ ]*' || :)
++TARGET_TRIPLE=$(echo "$LDFLAGS" | grep -o -- '--target=[^ ]*' | sed 's/--target=//' | tail -1 || :)
++if [[ "$(uname -s)" == "Linux" ]]; then
++    SAN_LDFLAGS="${FUSE_LD_FLAG} ${SYSROOT_FLAG}"
++    EXTRA_MAKE_ARGS+=("TARGET_AR=${AR} ${ARFLAGS:-rcus}")
++    if [[ -n "${TARGET_TRIPLE}" && "${TARGET_TRIPLE}" != *"$(uname -m)"* ]]; then
++        EXTRA_MAKE_ARGS+=("HOSTCC=gcc")
++    elif [[ -n "${FUSE_LD_FLAG}" ]]; then
++        EXTRA_MAKE_ARGS+=("HOST_LDFLAGS=${FUSE_LD_FLAG} ${SYSROOT_FLAG}")
++    fi
++    export TARGET_LDFLAGS="${LDFLAGS:-} -fno-function-sections -fno-data-sections"
++    export CFLAGS=""
++    export LDFLAGS=""
++else
++    SYSROOT_FLAG=$(echo "$CFLAGS" | grep -o -- '--sysroot=[^ ]*' || :)
++    if [[ -n "${SYSROOT_FLAG}" ]]; then
++        export HOST_CFLAGS="${SYSROOT_FLAG}"
++        export HOST_LDFLAGS="${SYSROOT_FLAG}"
++        EXTRA_MAKE_ARGS+=("HOST_CFLAGS=${HOST_CFLAGS}" "HOST_LDFLAGS=${HOST_LDFLAGS}")
++    fi
++    export TARGET_LDFLAGS="${CFLAGS:-} -fno-function-sections -fno-data-sections"
++    export CFLAGS=""
++    export LDFLAGS=""
++fi
++
++# s390x and ppc64le require -fno-integrated-as with clang
++ARCH=$(uname -m)
++if [[ "$ARCH" == "s390x" || "$ARCH" == "ppc64le" ]]; then
++    ASM_FLAGS="-fno-integrated-as -fgnuc-version=10 -Wno-implicit-function-declaration -D_Float32=float -D_Float64=double -D_Float128=double -D_Float32x=double -D_Float64x=double"
++    export TARGET_CFLAGS="${TARGET_CFLAGS} ${ASM_FLAGS}"
++    export HOST_CFLAGS="${HOST_CFLAGS:-} ${ASM_FLAGS}"
++fi
++
++export TARGET_STRIP="@echo"
++
++if [[ -n "${ENVOY_CONFIG_ASAN}" ]] || [[ -n "${ENVOY_CONFIG_MSAN}" ]]; then
++    export LDFLAGS="$SAN_LDFLAGS"
++    BLOCK_PATH=$(realpath clang-asan-blocklist.txt)
++    export TARGET_CFLAGS="${TARGET_CFLAGS} -fsanitize-blacklist=${BLOCK_PATH}"
++    echo "fun:*" > clang-asan-blocklist.txt
++fi
++
++"${MAKE:-make}" -j$(nproc) V=1 PREFIX="$PREFIX" \
++    BUILDMODE=static \
++    "XCFLAGS=-DLUAJIT_ENABLE_LUA52COMPAT" \
++    "${EXTRA_MAKE_ARGS[@]}"
++
++# Bypass the top-level `make install` target: under this sandboxed,
++# cross-compiling, parallel-make invocation its install: recipe does not
++# execute (no "Installing LuaJIT" banner is ever emitted), leaving lib/
++# empty even though src/libluajit.a was built successfully. Install the
++# static archive and public headers ourselves to match
++# out_static_libs=["libluajit-5.1.a"] and out_include_dir="include/luajit-2.1".
++mkdir -p "$PREFIX/lib" "$PREFIX/include/luajit-2.1"
++cp src/libluajit.a "$PREFIX/lib/libluajit-5.1.a"
++cp src/lua.h src/lualib.h src/lauxlib.h src/luaconf.h src/lua.hpp src/luajit.h "$PREFIX/include/luajit-2.1/"
