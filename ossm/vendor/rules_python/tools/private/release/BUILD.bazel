load("@rules_python//python:defs.bzl", "py_binary")

package(default_visibility = ["//visibility:public"])

py_binary(
    name = "release",
    srcs = ["release.py"],
    main = "release.py",
    deps = [
        "@dev_pip//packaging",
    ],
)
