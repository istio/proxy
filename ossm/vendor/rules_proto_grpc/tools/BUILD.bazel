load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier")

buildifier(
    name = "buildifier",
    lint_mode = "warn",
    lint_warnings = [],
    mode = "fix",
    visibility = ["//visibility:public"],
)
