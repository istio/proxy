initscript {
    dependencies {
        classpath(files("{{pluginJarPath}}"))
    }
}

allprojects {
    apply plugin: com.github.bazelbuild.rules_jvm_external.resolver.gradle.plugin.GradleDependencyModelPlugin
}