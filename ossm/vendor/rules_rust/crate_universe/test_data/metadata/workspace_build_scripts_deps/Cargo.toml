[workspace]
members = [
    "child",
]
resolver = "2"

[package]
name = "workspace-build-scripts-deps"
version = "0.1.0"
edition = "2021"

# Required to satisfy cargo but no `lib.rs` is expected to
# exist within test data.
[lib]
path = "lib.rs"

